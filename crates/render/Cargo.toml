[package]
name    = "voxel-render"
version = "0.1.0"
edition = "2021"

[dependencies]
voxel-core = { workspace = true }
bevy       = { workspace = true }

[dev-dependencies]
voxel-mapgen = { workspace = true }
